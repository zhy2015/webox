# WeBox 90-Minute Delivery TODO

本清单是当前唯一执行入口。目标是在 90 分钟内先完成一个可启动、可存储、可演示的企业级初版，再进行优化。任务优先级为 `P0 > P1 > PAUSED`；任何时刻不得用 P1 或优化工作阻塞 P0 端到端流程。

状态说明：`[ ]` 未开始，`[~]` 进行中，`[x]` 已通过完成标准，`[!]` 被环境或缺陷阻塞。只有执行了对应校验，才能标记为完成。

## 0. 当前前置状态

- [x] `PLAN-01` 需求、技术方案、时间盒和验收标准已经固化。
  - 完成标准：`README.md`、需求基线、技术方案、本 TODO 和验收矩阵之间没有失效链接。
  - 已验证：本地 Markdown 链接检查通过。
- [x] `ENV-01` 激活已经安装的 JDK 17。
  - 当前事实：默认 `java`/`javac` 为 Corretto 8；Homebrew OpenJDK 17.0.16 已安装在 `/opt/homebrew/Cellar/openjdk@17/17.0.16/libexec/openjdk.jdk/Contents/Home`。
  - 完成标准：`java -version` 与 `javac -version` 都显示 17；`mvn -version` 使用同一 JDK 17。
- [x] `ENV-02` 启动 Docker Desktop/daemon。
  - 当前事实：Docker daemon 已运行，MySQL Compose 服务健康。
  - 完成标准：`docker info` 成功，`docker compose version` 成功。

## 1. P0：必须完成的端到端初版

### 工程与数据库

- [x] `BOOT-01` 创建 Spring Boot 3.x / Java 17 Maven 工程。
  - 完成标准：`cd backend && ./mvnw test` 成功；应用启动后 `/actuator/health` 返回 `UP`。
- [x] `BOOT-02` 创建 React + TypeScript + Vite 工程。
  - 完成标准：`cd frontend && npm ci && npm run build` 成功；`npm run typecheck` 无错误。
- [x] `DB-01` 使用 Docker Compose 启动独立 MySQL 8。
  - 完成标准：容器健康；后端能连接；不得使用 H2、SQLite 或内存数据库替代。
- [x] `DB-02` 添加 Flyway 初始迁移与可重复演示种子。
  - 完成标准：空数据库启动时自动建表；重复启动不报错；当天存在可下单菜单、9 个英文示例菜品、员工和管理员演示账号。

### 后端业务闭环

- [x] `BE-01` 完成注册、登录、退出、当前用户和角色授权。
  - 完成标准：合法用户可注册登录；刷新浏览器后仍登录；密码为 BCrypt 哈希；重复邮箱/弱密码返回英文错误；员工访问 Console API 返回 `403`。
- [x] `BE-02` 完成每日菜单、搜索、分类筛选和菜品详情 API。
  - 完成标准：数据来自 MySQL；只返回已上架且加入当日菜单的菜品；搜索长度不超过 50；分类支持多选；价格以固定两位小数字符串返回。
- [x] `BE-03` 完成服务端价格与选项校验。
  - 完成标准：必选项缺失、未知选项、重复单选项被拒绝；附加价格完全由后端重算；不得接受前端传入总价作为订单金额。
- [x] `BE-04` 完成下单事务、数量上限、截单切换和重复餐次保护。
  - 完成标准：总份数最多 5；时间使用 `Asia/Shanghai` 服务端时钟；午餐过期切晚餐、晚餐过期切次日午餐；相同用户/日期/餐次最多一个有效订单。
- [x] `BE-05` 完成幂等和库存扣减。
  - 完成标准：相同用户和 `Idempotency-Key` 重复提交只生成一个订单；MySQL 行锁下库存不为负；库存不足返回具体菜品；事务失败无半成品订单。
- [x] `BE-06` 完成订单列表、详情和取消。
  - 完成标准：用户只能访问自己的订单；历史信息使用订单快照；仅 `Pending` 可取消；重复取消不会重复恢复库存。
- [x] `BE-07` 完成最小偏好保存与读取。
  - 完成标准：过敏原、菜系、辣度和预算可持久化；枚举值非法时返回英文校验错误。
- [x] `BE-08` 完成最小 Console 菜品与每日菜单管理。
  - 完成标准：管理员可新增、编辑、上下架菜品，并配置今天/明天的菜单和非负库存；员工无权限。

### 前端业务闭环

- [x] `FE-01` 完成登录、注册和角色路由。
  - 完成标准：错误反馈为英文；刷新恢复登录态；Employee 与 Console 导航隔离；无权限页面有明确反馈。
- [x] `FE-02` 完成响应式菜单页和菜品定制。
  - 完成标准：手机和桌面均无横向溢出；支持搜索、菜系多选、详情和必选/可选规格；全部用户文案为英文。
- [x] `FE-03` 完成购物车。
  - 完成标准：同菜品同配置合并、不同配置分行；增减删除正确；总量到 5 后禁用增加；金额无浮点误差；刷新后购物车仍在。
- [x] `FE-04` 完成结算、成功页和订单页。
  - 完成标准：一次点击生成幂等键并防重复点击；展示后端最终餐次和金额；可查看订单及取消 `Pending` 订单；错误可恢复。
- [x] `FE-05` 完成最小偏好页与过敏原提醒。
  - 完成标准：偏好可保存；加入命中过敏原的菜品时出现英文确认框；取消不加入，确认只加入一次；预算超限只提醒。
- [x] `FE-06` 完成最小 Console。
  - 完成标准：管理员可查看、搜索、新增、编辑、上下架菜品并配置每日菜单；操作后员工菜单刷新可见。

### 集成、测试和交付

- [x] `TEST-01` 后端核心规则测试。
  - 完成标准：价格计算、5 份限制、截单边界、有效餐次唯一、幂等、库存竞争、取消恢复均有自动测试并通过。
  - 已验证：11 个自动测试覆盖同菜品不同规格计价、汇总库存、库存调整保护、5 份限制、选项错误、幂等回放、餐次唯一与主动查询、取消恢复和截单边界；双客户端真实数据库竞争验证通过。
- [x] `TEST-02` 前端核心状态测试。
  - 完成标准：购物车配置身份、总量限制、金额计算、过敏原确认至少各有一个通过的自动测试。
- [x] `E2E-01` 执行真实 MySQL 的浏览器冒烟流程。
  - 完成标准：注册/登录 -> 菜单 -> 定制 -> 购物车 -> 下单 -> 订单详情 -> 取消全流程通过；另验证管理员修改菜品后员工端可见。
  - 已验证：真实浏览器完成员工下单、订单取消、管理员菜单保存、390 x 844 响应式和 `View existing order`；脚本自动验证注册校验、权限、偏好、管理员库存传播与恢复、多规格下单、幂等、并发和取消。
- [x] `DOC-01` 更新可执行 README、API 文档和 AI 对话原始记录。
  - 完成标准：新环境按 README 能启动；OpenAPI 可访问；演示账号和非秘密配置说明清楚；`ai-conversations/` 有完整原始导出。
- [x] `RELEASE-01` 最终干净环境校验。
  - 完成标准：MySQL 从空卷启动、迁移和种子成功；前后端构建成功；无明文密码/API Key；P0 冒烟测试通过。

## 2. P1：仅在 P0 冒烟通过后补充

- [ ] `P1-01` 菜品图片 multipart 上传到本地持久化目录；否则初版保留管理员填写图片 URL/预置素材路径。
- [ ] `P1-02` `For You` 确定性排序和辣度高亮。
- [ ] `P1-03` 历史地址选择；初版可从历史订单地址去重生成。
- [ ] `P1-04` Console 订单只读列表；不在初版实现复杂状态流转。
- [ ] `P1-05` 菜单分页或虚拟列表；当前约 50 条时先使用普通响应式网格。
- [ ] `P1-06` 更完整的错误边界、加载骨架、空状态和可访问性修正。

每个 P1 项的最低完成标准：相关主流程不回归、至少有一个针对新增行为的自动测试、README/API 契约同步更新。

## 3. PAUSED：90 分钟内明确停止

- [ ] `PAUSED-01` AI 流式推荐与真实 LLM 接入。
- [ ] `PAUSED-02` 经营数据看板。
- [ ] `PAUSED-03` SSE 实时库存推送；初版下单/取消后主动刷新库存。
- [ ] `PAUSED-04` Redis、分布式缓存、消息队列和 Outbox。
- [ ] `PAUSED-05` 微服务、Kubernetes、对象存储和多节点会话。
- [ ] `PAUSED-06` 邮件验证、忘记密码、复杂审计后台和管理员订单状态工作流。
- [ ] `PAUSED-07` 视觉微动效、复杂主题和非关键性能优化。

暂停项只有在 `RELEASE-01` 通过后才能重新进入 TODO。

## 4. 时间盒与停止线

| 时间 | 目标 | 强制停止线 |
| --- | --- | --- |
| 00-10 分钟 | 环境、前后端骨架、MySQL | 10 分钟时必须能开始编译；环境问题未解则记录阻塞，不做 UI |
| 10-35 分钟 | schema、种子、认证、菜单、下单后端 | 35 分钟时必须能用 HTTP 创建真实订单 |
| 35-60 分钟 | 员工端核心流程 | 60 分钟时必须能从浏览器完成一次下单 |
| 60-72 分钟 | 取消、偏好、最小 Console | 核心流程有缺陷时立即停止新增功能 |
| 72-82 分钟 | 自动测试、集成修复 | 72 分钟后禁止引入新依赖或架构改造 |
| 82-90 分钟 | 干净启动、冒烟、README、交付记录 | 82 分钟后只允许修复阻断演示的问题 |

全局停止规则：任何 P1 工作影响 P0 时立即回退 P1；同一非核心问题连续修复两次仍失败时记录为已知问题并绕过；不得为了“看起来完整”伪造未落库数据或跳过服务端校验。
